using UnityEngine;
using UnityEngine.UI;

namespace CatJam
{
    public sealed class ScoreView : MonoBehaviour
    {
        //[SerializeField] private TextTMP _scoreText;

        public void UpdateScoreText(int score)
        {
            //_scoreText.text = $"Score: {score}";
        }
    }
}